<h1>{{job.title}}</h1>
<h4>{{job.category}}</h4>
<small>
{{#each job.skill}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
</small>
</br>
<a href="" target="_blank">Go there -></a>
</br>
<h2>sessionUserId => {{sessionUserId}}</h2>
<br/>
<h2 style="color:tomato">{{userIsInterested.interest}}</h2>
<p>But Application element.interested_user._id is {{application.interested_user._id}} </p>
<p>And sessionUserId is {{sessionUserId}} </p>
<br/>
{{#if userIsInterested.interest }}
    <p> {{sessionUserId}} interest is true {{interest}}</p>
{{else}}
    <p> {{sessionUserId}} interest is false {{userIsInterested.interest}}</p>
{{/if}}

 <form method="POST" action="">
    <button>I´M INTERESTED</button>
    <input type="text" id="input-title" name="job" value="{{job._id}}" required />  

  </form>
{{#if job.image}}
  <img src="{{job.image}}" alt="{{job.title}}">
{{/if}}
</br>
<a href="/job/{{job._id}}/delete">Delete job</a>
</br>
<a href="/job/{{job._id}}/update">Update job</a>
{{#each application }} 
<small>{{interested_user.firstname}} </small>
<small>{{interested_user._id}} </small><br>
{{/each}}